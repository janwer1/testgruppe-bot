{
  "name": "telegram-join-request-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "Production-ready Telegram bot for join request review",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "bun run --watch src/dev.ts",
    "dev:worker": "wrangler dev",
    "dev:debug": "bun --inspect --watch src/dev.ts",
    "dev:inspect": "bun --inspect src/dev.ts",
    "start": "wrangler dev",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "webhook:setup": "bun src/scripts/setup-webhook.ts",
    "webhook:get": "bun src/scripts/get-webhook.ts",
    "webhook:test": "bun src/scripts/test-webhook.ts",
    "postinstall": "bun run webhook:setup",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@fluent/bundle": "^0.19.1",
    "@std/ulid": "npm:@jsr/std__ulid",
    "@t3-oss/env-core": "^0.13.10",
    "@upstash/redis": "^1.36.1",
    "dotenv": "^17.2.3",
    "grammy": "^1.39.3",
    "pino": "^10.3.0",
    "pino-pretty": "^13.1.3",
    "xstate": "^5.25.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.60.0"
  },
  "engines": {
    "node": "24.x",
    "bun": "1.x"
  }
}